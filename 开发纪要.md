# MHRS 竞速成绩库 · 开发纪要

> 本文档用于**快速接续开发上下文**。改代码前先看这里 + `README.md`；重要约定变更请同步更新本文档。
> 最后更新：2026-09-07（主题切换完成；成绩数据已由网页录入 8 条真实成绩）

## 1. 项目是什么

怪物猎人 崛起：曙光（MHRS）竞速成绩收录网站。
- 线上地址：https://absolute-balance.github.io/MHRSpeedrun/
- 仓库：https://github.com/Absolute-Balance/MHRSpeedrun（main 分支，GitHub Pages 自动部署，提交后约 1~2 分钟生效）
- 本地目录：`C:\Users\Administrator\Downloads\mhrspeedrun`（同时是 git 仓库）
- 站长 B站主页按钮：@星空柠檬凛 → https://space.bilibili.com/35665642

## 2. 技术架构（重要）

- **纯静态**：HTML + CSS + JS，无构建步骤、无后端、无数据库
- 数据以 **JS 文件**存储：`js/data.js`（唯一数据源，网页「录入成绩」保存时直接改 GitHub 仓库里这个文件并提交，触发 Pages 自动重新发布）
- 图标：`weapons/`（14 张中文名 png）、`monsters/`（68 张中文名 png）
- 怪物映射自动生成：`data/monster-order.json`（顺序+分组）→ `tools/scan-monsters.js` → `js/monsters.js`（改名/增删后重跑 `node tools/scan-monsters.js`）
- 本地预览：`node tools/serve.js` → http://127.0.0.1:8123

## 3. 界面逻辑

- **任务类型单选**：烈祸袭来 / 怪异探究Lv300 / 特别探究
  - 烈祸袭来：横轴固定 10 个任务（config.js `ragingQuests`，q01~q10，每个任务绑定目标怪）
  - 两种探究：勾选 EX1~EX9 或 **Apex**（霸主 6 只，半独立档排 EX9 后、蓝色），横轴=对应怪物；>10 自动分页
- 纵轴固定 14 种武器；格子=该位置**最快的一条**（时间+玩家，无“历史 n”角标；历史在点进格子后看）
- 点击行为：
  - 怪物头像 → 全武器最快页
  - 成绩格 → 单武器页（当前记录大卡 + 历史记录）
  - 玩家名 → 玩家时间线页（按日期，可展开视频）
  - 空白格「＋」→ 快速录入；单武器页「＋ 为该位置录入成绩」同理
- 右上角 ☀️/🌙 主题切换（白天=浅色暖纸；存 localStorage `mhrs_theme`，图例文字随主题变化）
- 「📜 收录规则」按钮展示收录条件；「⤓ 导出表格图片」按当前筛选生成 PNG（固定深色风格）
- 筛选状态写进 URL（qt/ex/rl/v/mid/wid/p 等），可分享

## 4. 数据字段（js/data.js 内 window.MHRS_RECORDS）

```js
{ id:'r…', questType:'raging|anomaly300|special',
  quest:'q01~q10'/*仅烈祸*/或 null, exStar:'EX1~EX9|Apex'/*仅探究*/或 null,
  rule:'sanyou|ta|free', monsterId:'m01~m68', weaponId:'gs|ls|…|bow',
  timeMs:毫秒, author:'玩家', date:'YYYY-MM-DD',
  videos:[{site:'bilibili', url:'https://www.bilibili.com/video/BV…', title:'视频标题'}],
  platform:'steam|switch|ps5', note:'' }
```

- 时间全站显示格式：**分'秒''百分秒**，如 `05'02''52`（分钟补零）；录入兼容 `5:47.33`、`6'57''52`、`4:52` 等写法
- 视频只收 B 站；**可只填 BV 号自动补全**；按钮文字用录入的「视频标题」，空则显示“打开视频”
- 规则时间颜色（夜晚）：**白色=三无规则、黄色=TA规则**（白天主题对应深色/金色，图例自动改文案）。「无限制规则」赛道已按站规移除（2026-09-07），界面仅保留 三无规则/TA规则 两档

## 5. 怪物体系（68 只）

- EX1~EX9：57 只（怪异克服系在 EX8/EX9；EX9 含 怪异克服天廻龙/天彗龙）
- 烈祸独有：月迅龙/焰狐龙/岚龙/冰龙/原初形态爵银龙
- Apex 档（霸主 6 只，mod 探究任务）：霸主·青熊兽/雌火龙/火龙/泡狐龙/角龙/雷狼龙（名字带间隔点，文件名同）
- 烈祸袭来 10 任务 ↔ 目标怪映射在 config.js `ragingQuests`（朦胧之影=月迅龙 … 冰狼长嚎=冰狼龙）

## 6. 成绩管理与权限（管理员机制）

- 录入入口：空白格「＋」/ 单武器页按钮；修改/删除按钮**仅当浏览器存有 GitHub 令牌时**显示（位置：当前记录卡、历史/玩家行展开详情底部）
- 令牌：GitHub → Settings → Developer settings → **Fine-grained tokens** → 仓库 `MHRSpeedrun` → Contents **Read and write**；填入录入弹窗后存 localStorage（key `mhrs_gh_token`）；可多个管理员各持一个令牌，可单独吊销
- 保存流程：先读 GitHub 最新 data.js → 合并/修改/删除 → 提交 → **自动把 index.html 资源版本号换新**（?v=…，防缓存）→ 1~2 分钟上线
- ⚠️ 曾踩坑：手动在 GitHub 网页编辑 data.js 不会自动换版本号，普通刷新可能看到旧数据 → 用 Ctrl+F5/无痕；且不要把 `[\n]` 之类字面量复制进文件

## 7. 收录规则（页面「📜 收录规则」同文）

只收录制相对完整的片；只收国内玩家；未展示装备/技能不收；不合法炼化不收；影响局内 MOD（显血、彩鬼火鸟等）不收；恶性老金玩家不收；未开伤害显示不收。

## 8. 部署与日常

- 更新=改文件后推送（`git push origin main`），Pages 自动发布
- ⚠️ 本机直连 GitHub 偶发 `Connection was reset`：试 `git -c http.version=HTTP/1.1 push origin main`、换热点网络，或**浏览器上传文件覆盖**（只传 css/style.css、js/app.js、index.html，勿动 js/data.js）
- 数据备份=git 历史（每次保存都是 commit，误删可回滚）；会话记录在 DSH 侧（见 README 说明？——详见 DSH session 文件，不随仓库同步）

## 9. 当前状态与待办

- [x] 矩阵视图/详情页/玩家页/录入/管理（改删）/导出图片/收录规则/主题切换
- [x] 成绩数据已清空重录：线上现有 8 条真实成绩（怪异克服天彗龙 EX9 多武器三无规则：__Ciel___、15soyoの睦头、-橘-孜-、BFeather7、郁_葱、-下机下机-、20guns_、K----J）
- [ ] 主题切换推送是否已成功上线的确认（如未上线按第 8 节处理）
- [ ] 木质任务板等视觉主题改造（需站长提供素材）
- [ ] 访客投稿后端（管理员+令牌方案已够现阶段用，开放投稿再上后端）
- [ ] 本地 data.js 与线上同步（本地预览落后于线上时 `git pull` 或重新 fetch）
